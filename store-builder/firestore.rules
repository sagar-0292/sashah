rules_version = '2';
// All reads and writes go through the Sahaay Stores API (firebase-admin),
// which bypasses these rules. Browsers never touch Firestore directly.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
