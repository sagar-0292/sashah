/* Sahaay Stores — platform configuration.
 *
 * firebase: paste your Firebase web app config here (Project settings →
 * General → Your apps → Web app). It is safe to publish; it only identifies
 * the project. Leave it null to run without hosting:
 *   • on the local dev server (node dev-server.js) a built-in test sign-in is used
 *   • on a plain static host (e.g. GitHub Pages) the builder runs in
 *     "download & self-host" mode, with everything saved in the browser.
 */
window.SAHAAY_CONFIG = {
  firebase: null
  // firebase: {
  //   apiKey: '...',
  //   authDomain: 'your-project.firebaseapp.com',
  //   projectId: 'your-project',
  //   appId: '...'
  // }
};
