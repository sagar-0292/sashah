# Copy to .env.local for local development (these values match the local stack:
# `pnpm stack` from the shopfront folder). For Vercel, see SETUP.md.

# Supabase project address and its public ("anon" / "publishable") key.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1sb2NhbCIsInJvbGUiOiJhbm9uIiwiZXhwIjoyMDAwMDAwMDAwfQ.Hs-FTAfDHn98HElrDsmKDrbzquR95nSPFBM1T2uUk5U

# Database connection using the limited "shopfront_app" login. SECRET.
DATABASE_URL=postgres://shopfront_app:app-local-only@127.0.0.1:5432/shopfront

# Production only (see SETUP.md):
# DATABASE_CA_CERT=          # Supabase's SSL certificate, so the connection is verified
# NEXT_PUBLIC_APP_URL=https://studio.youragency.in
# RESEND_API_KEY=            # sends invitation emails. SECRET.
# EMAIL_FROM=Mumbai Web Studio <studio@youragency.in>
